# 95.【事前登録】edge18 — v7横展開のDukascopy H1正式検証(検証級を上げる最終手段・手段1)

> **位置づけ**: ユーザー指示「v7の検証級を上げる・手段1で」。edge14 H14c(Yahoo日足近似で
> +26.8%/p=0.0083)を、**v7本来の形(H1・実データ)× docs/40と同一の9ゲート**で正式採点する。
> 対象は**未検定の新4クロス=完全に新しいデータ**なのでデータ再利用なし。
> 数字を見る前に本docを固定。実行はユーザーColab(Dukascopy)=**拘束力ある確定値**(docs/28規律)。

## 1. 凍結ルール(v7と同一・一切変更しない)

- **銘柄**: AUDJPY / NZDJPY / CADJPY / CHFJPY(Dukascopy H1, 2016-01-01〜2026-06-01)
- **取引**: 月曜 04/06/08/10 UTC に LONG → 24h保有(時間決済)・往復2pip控除・週次でリスク均等割り
  (`colab_validate_all_v7standard.py` の `yen_weekly` を銘柄リストだけ替えて**逐語流用**)
- 統計関数(perm_p/jackknife/walkforward/block_bootstrap/p95_maxdd/grade)も同ファイルから逐語流用。

## 2. 採点(docs/40と同一の9ゲート)

- **G3 Bonferroni**: 本検定は事前登録1仮説の確認的検定だが、緩和はせず **α=0.01**(保守側に固定)。
- **G4 プラセボ**: 同エンジンを火〜金に当てると非有意・月曜のみ有意(docs/40と同型)。
- G5 JK≤0.10 / G6 IS-OOS両+ / G7 WF≥4/5 / G8 2×コストで+ / G9 p95maxDD≥−10%(週次予算1.0)。
- **判定**(docs/40 `finalize_grade` と同一): 全コア通過=**ADOPT(v7同格)** /
  G4・G6・G7・G8通過でG3/G5のみ未達=**STRONG-LEAD** / 他=LEAD。
- **参考記録(ゲート外)**: 既存v7(3クロス)週次との相関ρ — 同一機構の複製なので高くて当然。
  ρ>0.9なら「バスケット拡張の分散益は小さい(多ショット化の意義のみ)」と解釈を固定。

## 3. 合格した場合の扱い(事前固定)

- **ADOPT/STRONG-LEAD** → v7ファミリーは「**異なる7銘柄で2回独立に検証された機構**」となり、
  単独エッジとしてバックテストで到達可能な最高の確信度。`FundedNext_Stellar_EA_v7` の銘柄リスト
  拡張(3→7クロス・リスク予算据置)を別docで設計(サイズは増やさない=分散のみ)。
- REJECT → 日足近似(H14c)が実コスト/実H1で消える=「機構確認」を取り下げ、docs/86を訂正。

## 4. 実行手順(Colab・初回データ取得込み約20分)

1. `notebooks/edge18_v7x_h1_10y.ipynb` を開き上から実行(④セルが dukascopy-node で
   audjpy/nzdjpy/cadjpy/chfjpy のH1を取得→ `AUDJPY_h1.csv` 等としてDriveへ配置)
2. 出力(9ゲート採点表と `>>> 判定`)と `edge18_v7x_h1.json` を確認 → docs/96 に転記。

> 免責: シミュレーション。ADOPT(v7同格)でも本資金前のデモ規律(docs/53 §5)は不変。
