# 29. E5（リスクパリティ多資産トレンド）デモ追検計画 ＋ −10%口座サイジング

> docs/27の唯一筋の良いLEAD=E5を、本資金前にデモで追検するための実装と判定基準。
> ステータス: **LEAD（検証済みエッジではない）。本資金禁止＝デモ/極小サイズの衛星のみ。**
> 実装: `mql5/Chien_E5_RP_Trend_EA.mq5`。サイジング根拠: `research/edge6_untried_10y.py`(E5)再構成。

## 0. E5とは
金+株価指数（XAUUSD/US500/NAS100/GER40）の**時系列モメンタム（TSMOM）を逆ボラ加重**した月次戦略。
過去1/3/6/12ヶ月リターン符号の多数決で各銘柄の方向を決め、リスクパリティ（各レッグの月次P&Lσを
均す）で建てる。MA2（docs/23）の上位互換で、**10年でOOS減衰なし・v7相関≈+0.07（分散先）**。

## 1. 素の10年実測（月次・docs/27 由来）
| 指標 | 値 |
|---|--:|
| 純益(10年) | +70.9% |
| CAGR | 6.1% |
| 年率ボラ | 11.5% |
| Sharpe | 0.57 |
| **maxDD** | **−17.8%** |
| 勝率 | 66% |

→ **素のmaxDD−17.8%はプロップ−10%枠と非両立**。サイズを落として器に収める必要がある。

## 2. −10%口座向けサイジング（maxDDは予算にほぼ線形）
目標maxDD≈**−6%**（−10%に4%マージン、v7と同基準）。スケール **k = 6.0 / 17.8 ≈ 0.337**:

| 項目 | スケール後 |
|---|--:|
| 想定maxDD | **−6.1%** |
| CAGR | 約 2.2% |
| 年率ボラ | 3.9% |
| **月次ボラ** | **約 1.12%** |
| $50k口座での月次変動(±1σ) | 約 ±$562 |
| $50k口座での10年最悪月 | 約 −$1,546 (−3.09%) |

**EAへの落とし込み**: 4レッグ独立・各 **`InpLegMonthlyRiskPct = 0.55%`**（月次P&Lσ＝equityの0.55%/レッグ）
→ ポート月次ボラ≈1.1% → 想定maxDD≈−6%。`Chien_E5_RP_Trend_EA.mq5` の既定値がこれ。
> ⚠ これは素のバックテスト由来の試算。**実maxDDがデモで−6%前後に収まるか監視し、超えるなら
> `InpLegMonthlyRiskPct` を下げる**（maxDDは概ね線形）。

## 3. v7 × E5 合成効果（ボラ正規化・重複34ヶ月・月次相関 +0.066）
| 配分 v7:E5 | Sharpe | CAGR | maxDD | 勝率 |
|---|--:|--:|--:|--:|
| 100:0 (v7単体) | 1.47 | 15.2% | −3.7% | 62% |
| 85:15 | 1.60 | 14.5% | −4.3% | 68% |
| **75:25** | **1.67** | 13.9% | −5.5% | 71% |
| **65:35** | **1.70** | 13.4% | −6.7% | 68% |
| 50:50 | 1.66 | 12.5% | −8.5% | 74% |

- **スイートスポット≈ E5配分25〜35%**（Sharpe 1.47→1.67〜1.70）。**改善は緩やか**（相関が負でなく≈0のため）。
  劇的な分散ではない点を誇張しない。勝率は上がる（62%→68〜71%）。
- ⚠ 重複が**34ヶ月のみ**（v7月次はローカルH1 2.76年由来）。**フェアな10年×10年の合成はv7も10年月次で
  再計算して確定**すること（`compare_v7_ma2()`の枠組み/Drive）。

## 4. デモ前進検証プロトコル（本資金移行の判定）
**目的**: 「正の期待値」と「v7低相関」が**フォワード（未来データ）で再現するか**を、損失を限定して確認。

### 設定
- 口座: **デモ**（または本資金でも極小=規定リスクの1/2以下）。Chien_E5_RP_Trend_EA を
  XAUUSD/US500/NAS100/GER40 の**各チャートに1つずつ**アタッチ。`InpLegMonthlyRiskPct=0.55`。
- 期間: **最低6ヶ月、できれば12ヶ月**（月次戦略=サンプル確保に時間要）。
- 並行して v7 も同口座/別口座で稼働し、**実現相関**を測る。

### 合格（本資金衛星に昇格）ゲート — 全て満たすこと
1. **期待値**: デモ期間の純益 > 0（コスト/スワップ/配当調整・実約定込み）。
2. **DD**: 実現maxDD ≤ 想定の1.5倍（≦ −9%）。超えたら即減量または停止。
3. **相関**: v7との月次相関 ≤ +0.3（分散先である前提が崩れていない）。
4. **整合**: 方向シグナルの勝率・挙動がバックテスト（勝率66%近辺）と乖離しない。
5. **約定健全**: 指数/金CFDのスプレッド・スワップ・ギャップが想定を致命的に悪化させない。

### 不合格 → 見送り（v7一本に戻す）
- いずれか1つでも明確に外れたら**本資金投入しない**。LEADを無理に採らない（docs全体の規律）。

## 5. 段階的ロードマップ
1. **今**: デモでEA稼働（legRisk 0.55%）。月次ログでDD・相関・勝率を記録。
2. **3-6ヶ月**: 中間レビュー。DDが−6%圏か、相関が低いかを確認。逸脱あれば減量。
3. **6-12ヶ月**: 上記ゲート判定。合格なら**本資金でv7:E5=75:25〜65:35**の衛星配分から開始。
4. 以降: v7主力＋E5衛星で運用。E5は四半期ごとにDD/相関を再評価。

> 免責: E5はLEAD（未検証）。数値はシミュレーション（Yahoo日足・月次終値・price index）であり将来を
> 保証しない。指数/金CFDの実コストはデモで要確認。本資金投入はデモ合格後・小サイズから。
