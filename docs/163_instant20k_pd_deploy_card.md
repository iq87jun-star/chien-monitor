# 163.【運用カード・確定】FN Stellar Instant $20K × PD間引き 1.0x(ワンクリック v1.33)

> 最終配置(2026-07-16ユーザー決定・docs/161の書面回答に基づく):
> **FN200k=季節R4G3(変更なし) / FN100k#14074882=季節R4G3(変更なし) / Instant20k=PD間引き1.0x /
> FTMO=季節R4G3**。チャレンジ間の同一EAは公式に可(docs/161 §5)、Instant↔チャレンジのみ同一取引不可
> (§4③)のため、Instantにだけ別戦略(PD間引き)を充てる。
> 対象EA: `mql5/【FNインスタント20k】PD間引き1.0x.mq5`(P1残りPD v1.32ベース+インスタント適合)。
> ⚠ `【FNインスタント20k】季節R4G3_0.75x.mq5`(docs/160)は**規約上使用不可**として警告焼き込み・廃止。

## 1. 期待値(docs/162・2万パスMC・研究1.0x)

| 項目 | 値 |
|---|--:|
| 昇格(+10%出金)中央 | **16ヶ月**(成功パスのみ) |
| 昇格成功率 / 失格が先 | **73.8% / 26.2%** |
| 失格12ヶ月(建値ロック) | 14.7% |
| 月次平均(研究1.0x) | ≈+0.6%(≈$120/月) |
| 昇格1回の期待掛金 | $720÷0.738 ≈ **$976** |

## 2. v1.32からの変更点(インスタント適合)

1. **トレーリングDDガード**: FNフロア=min(HWM−6%×初期, 建値)をEA内で複製。フロア+2%で新規停止
   (回復で自動再開)・+1%で全決済+恒久停止。HWMは端末保存(`ChienPD_hwm_*`)で再起動・再アタッチ耐性。
2. **利益ロック/+8%停止を無効**(目標なし)。**+10%到達でプッシュ通知**→出金→昇格→`InpBaselineReset=true`。
3. **サイズ=研究1.0x**: v7週次0.38% / v4 0.11%/回 / E-Mon週次0.38% / E5レッグ0.61%
   (3倍相当版1.09/0.32/1.09/1.75 ÷ 2.853)。日次−4%・休日フィルタ・ペア別スプレッド上限は維持。
4. 全レッグSL付き(v7/EMon=2.5×ATR災害SL・v4=1.5ATR・E5=2.5×ATR(MN1))
   → **Max Risk 3%(SL基準・トレード個別)に構造適合**。1.0xの1トレード最大はE5≈1.5%<3%。
5. Magic基底760920(P1残り版760720と別・同居しても衝突しない)。静的フロア−5%は最終バックストップ。

## 3. 運用手順

1. 購入(Swap-Free+VPS & EAアドオン)→ VPSのMT5・**新チャート1枚**にドロップ→OK(設定変更不要)。
2. 初週: `[HWM]`(20,000/フロア18,800)・エントリーログのロット・`ChienRiskLog`のopen-riskを確認。
3. +10%通知 → FNダッシュボードで出金申請(累計10%+出金1回で昇格)→ 昇格反映後に
   `InpBaselineReset=true`を一度だけ(基準残高とHWMを新tierで取り直し)。
4. 月次でforward scoring(docs/103)に実測を記録(分布は`pd_pruned`を1.0x換算で使用)。

## 4. 誠実な注記

- 成功73.8%=**26.2%は掛金$720を失う**。昇格は季節案(中央8ヶ月)より大幅に遅い16ヶ月 —
  これは規約制約下での次善であり、「Instantに残り物」という配置の帰結(docs/161 §5)。
- 月内トレーリングT2(`InpMTrailEnable`)は既定OFFのまま(デモ未了)。
- Yahoo校正=v4含む構成は実データ比で保守側(docs/138)。トレーリングMCは日次close解像度=実失格やや高い側
  (フロア+1%緩衝で防衛)。シミュレーションであり将来を保証しない。
