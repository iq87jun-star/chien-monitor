# 出品前チェック — 定石 参 ─ 値幅計

作成: 2026-08-28
状態: **制作物は揃っています。残るのはあなたの手元の作業だけです。**

---

## この商品は何か

**次のバーがどれだけ動くかを表示する道具。方向は出さない。**

`fx-ea/search/` の探索で唯一基準を通った知見を商品にしたものです。
同じ仕組みで「翌日の方向」を当てにいった仮説は46件すべて棄却され、
「翌日の値幅」だけが通りました。

---

## 出品するファイル

| ファイル | 場所 |
|---|---|
| `定石参_値幅計.mq5` | `indicator-next/MQL5/` |
| `定石参_値幅計.mq4` | `indicator-next/MQL4/` |
| 商品名・キャッチコピー・商品概要・取扱説明書 | `indicator-next/SALES.md` |
| 登録フォーム入力シート | `indicator-next/FORM.md` |
| 根拠数値(社内用) | `indicator-next/EVIDENCE.md` |
| 表示イメージ図(参考) | `indicator-next/mockup/` |

**成績数値はすべて実測済みです。実測待ちはありません。**

---

## あなたの作業

| # | 作業 | 備考 |
|---|---|---|
| 1 | MetaEditor で mq5 / mq4 をコンパイル | **未実施。こちらではコンパイルできません** |
| 2 | 自分のMT5/MT4で表示を目視確認 | D1 と H1 の両方で見てください |
| 3 | **自己採点がプラスで出るか確認** | ここが商品の主張。マイナスなら出品を止めてください |
| 4 | スクリーンショット撮影(4枚) | `FORM.md` の12番 |
| 5 | 商品登録 → ファイルアップロード → **自分で販売開始** | `FORM.md` を上から貼る |

### 3番が最重要です

商品概要に「平年並みより誤差を減らす」と書いています。
**あなたの口座・あなたの時間足で、自己採点がマイナスに出るなら、
その主張は成立していません。** その場合は連絡してください。出品を止めます。

確認する時間足: **D1(検証済み)と、実際に使う時間足**。
検証は日足でしか行っていないため、下位足でマイナスになる可能性はあります。
その場合でも、商品概要にその旨を書いてあるので出品自体は可能ですが、
どの時間足で使えるかを把握してから出したほうが問い合わせが減ります。

---

## 未確認のこと

| 項目 | 状態 |
|---|---|
| MetaEditor でのコンパイル | **未実施**。構文エラーが出たら内容を教えてください |
| MQL4版の動作 | **未実施**。MQL5版から機械的に移植したものです |
| 日足以外の時間足での精度 | **未検証**。自己採点で各自確認する設計 |
| WEB認証のMQL5対応 | 環境計と同じく運営に確認中 |

---

## 環境計との関係

重複する表示は「想定変動幅」だけです。

| | 定石 零 ─ 環境計 | 定石 参 ─ 値幅計 |
|---|---|---|
| 答える問い | いま入ってよいコストか | どれだけ動きそうか |
| 中心 | スプレッドの時間帯別割高度 | 値幅の予測と自己採点 |
| 精度の主張 | しない | **する(20銘柄15年の実測)** |

商品概要に「両方を買う必要はない。どちらか一方で足りる」と明記してあります。
**これは意図的です。** 重複を隠して2本売ると、レビューで必ず指摘されます。

出す順番は、環境計 → 待ち伏せ → 値幅計 を推奨します。
値幅計は精度の主張がある分、レビューでの検証圧が高くなります。
実績とレビューがいくらか付いてからのほうが安全です。

---

## 数値を直すとき

商品概要の数値(平均+18.5% / 最良+37.7% / 最悪+7.1% / 20銘柄 / 15年)は
`EVIDENCE.md` から取っています。

```
cd fx-ea/search && python3 mirror_rangemeter.py --evidence
```

これは**出荷するコードと同じ計算**を回して EVIDENCE.md を書き直します。
**手で数値を書き換えないこと。** 出荷するコードが出していない数字を
販売ページに載せないための仕組みです。

---

## やらないこと

- 方向・売買シグナルを示唆する表現を使わない
- 「必勝」「無敗」等の断定表現を使わない
- 実測していない時間足の精度を書かない
- 無期限の月額課金にしない(出品不可)
- 出品・販売開始の操作を代行しない(あなたが行う)
