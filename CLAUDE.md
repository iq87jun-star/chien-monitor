# chien-monitor

React + Vite のアプリ。現在のエントリポイントは `src/KakeiboApp.jsx`(収支きろく=家計簿)。
`src/App.jsx`(ちえんモニター)は未完成のまま保留中。

## 収支記録の運用ルール

ユーザーがチャットで収入・支出を伝えてきたら(例:「今日 食費 1200円 スーパー」)、
`public/entries.json` に以下の形式で追記し、コミットしてプッシュすること。

```json
{
  "id": "chat-20260724-001",
  "date": "2026-07-24",
  "type": "expense",
  "category": "食費",
  "amount": 1200,
  "memo": "スーパー"
}
```

- `id` は一意にする(`chat-YYYYMMDD-連番` 推奨)。既存の id と重複させない。
- `type` は `"income"`(収入)か `"expense"`(支出)。
- `category` は次から選ぶ(合わなければ「その他」):
  - 収入: 給料, ボーナス, 副収入, 臨時収入, その他
  - 支出: 食費, 日用品, 住居, 水道光熱, 通信, 交通, 医療, 交際, 趣味, 教育, その他
- 配列は日付昇順を保つ。
- 日付の指定がなければ当日(JST)とする。曖昧な場合のみユーザーに確認する。
- 記録後は「何を記録したか」と「累計・当月の収支」を簡潔に返信する。

アプリ側は起動時に `/entries.json` を取得し、ブラウザの localStorage の記録と
id で重複排除して統合する(`src/KakeiboApp.jsx` 参照)。
